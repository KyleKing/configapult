"Remove if found txt Jinja" - file and dir! ^No json, just lines of paths that are templated and deleted!

How to handle versioning? Like Alembic with multiple files? But if so, how to know which version is catapulted? Maybe from the local toml when applied?

Multiple small functions that yield are run with independent variable scopes and can therefore be composed
